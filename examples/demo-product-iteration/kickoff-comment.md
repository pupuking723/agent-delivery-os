<!-- delivery-os-kickoff -->
Mode label detected: `product-iteration`

Project: `airi-web`
Workspace root: `client-workspaces`
Suggested workspace path: `client-workspaces/34-团队邀请功能二期`

Use this command to generate the workspace for this issue:

```bash
pnpm kickoff:issue -- --repo pupuking723/agent-delivery-os --issue 34 --out client-workspaces/34-团队邀请功能二期
```

If you need to overwrite an existing workspace, append `--force`.

